# conversão de tipos, coerção
# type convertion, typecasting, coercion
# é o ato de converter um tipo em outro
# tipos imutáveis e primitivos: str, int, float, bool(uma vez criados ,não há como alterar. os tipos)
# print: é uma função

print(int("1"), type(int("1")))# fiz isso, para converter um string em um número inteiro.
print(type(float("1") + 1)) # converti em inte, antes de somar
print(bool(" "))
print(str(11) + "b") #converti o 11, em string,para concatenar
